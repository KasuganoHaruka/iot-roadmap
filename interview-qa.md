# 工业物联网面试题库(带答案要点)

> 用法:**每题出声讲一遍**,不要默读。讲不顺的标记 ❌ 回去补。
> 面试官在 15–20k 这个档位考的不是「知不知道」,而是「**讲不讲得清、踩没踩过坑**」。
> 每个答案后面尽量挂一个你自己的项目细节 —— 这是 10 年经验的人和应届生的差别。

---

## 一、Modbus(必考,你的主场)

### Q1. 功能码 03 和 04 有什么区别?
- 03 = 读保持寄存器(Holding Register,4x 区),**可读可写**
- 04 = 读输入寄存器(Input Register,3x 区),**只读**
- 四个区完整对应:线圈 0x(读写位,FC01/05/0F)、离散输入 1x(只读位,FC02)、输入寄存器 3x(只读字,FC04)、保持寄存器 4x(读写字,FC03/06/10)
- **实战补充**:很多国产设备偷懒,把所有数据都塞在保持寄存器里,只支持 03。所以拿到新设备第一件事是看手册的寄存器表,不要想当然。

### Q2. 一次最多能读多少个寄存器?
- FC03/04:**125 个寄存器**(0x7D)。因为 RTU 帧最大 256 字节,响应 = 1 地址 + 1 功能码 + 1 字节计数 + N×2 数据 + 2 CRC
- FC01/02:**2000 个线圈**(0x7D0)
- FC10 写多个寄存器:**123 个**(0x7B);FC0F 写多个线圈:1968 个
- **实战补充**:批量读优化时必须夹在这个上限里。我的做法是按「最大跨度 + 上限」两个条件切分请求,把离散地址合并成最少请求数 —— 轮询周期能降一个数量级。

### Q3. CRC 为什么低字节在前?
- Modbus 的数据域是**大端**(高字节在前),但 CRC16 是**小端**(低字节在前)。这是规范里的历史遗留不一致,不是逻辑推导出来的,记住就行。
- CRC16 参数:多项式 `0xA001`(即 `0x8005` 的反转)、初值 `0xFFFF`、无异或输出
- **加分**:Modbus TCP 没有 CRC(TCP 自己保证),换成 7 字节 MBAP 头:事务 ID(2)+ 协议 ID(2,恒为 0)+ 长度(2)+ 单元 ID(1),端口 502。**MBAP 头全部是大端**。

### Q4. 32 位浮点数读出来是乱码,怎么排查?
- 这是**字序(word order / byte order)**问题。两个 16 位寄存器组成 32 位,有四种排列:ABCD(大端)、CDAB(字交换,最常见)、BADC、DCBA(小端)
- 排查方法:让设备输出一个**已知值**(比如温度 25.5),把 4 个字节的所有 4 种排列都算一遍,哪个等于 25.5 就是哪种
- **实战补充**:所以我的驱动把字序做成点表里的可配置项,而不是写死。现场换个品牌的仪表就不用改代码。

### Q5. 485 总线能挂多少台设备?终端电阻怎么装?
- 标准收发器 **32 个单位负载**;用 1/8 单位负载的芯片可以到 **256 台**
- 距离:1200 米 @ 100kbps,速率越高距离越短(经验值:速率 × 距离 ≈ 常数)
- **终端电阻 120Ω 只装在总线物理两端的两台设备上**,中间的绝对不能装 —— 装多了阻抗过低,信号幅度不够,表现为随机丢包
- 拓扑必须是**手拉手菊花链**,不能星型、不能分叉长支线
- **实战补充**:现场最常见的三个问题就是「星型接线、终端电阻装了三个、屏蔽层两端都接地形成地环流」。

### Q6. 地址老是差 1,怎么回事?
- 传统 PLC 文档用 **1-based 的 4xxxx 记法**(40001 表示第 1 个保持寄存器),而协议报文里是 **0-based**(起始地址 0)
- 所以文档写 40001,报文里填 0;文档写 40100,报文里填 99
- 有些设备手册直接给协议地址,有些给 4xxxx 地址,**看到 4 开头 5 位数就要警觉**

---

## 二、通信与可靠性

### Q7. 串口/TCP 粘包怎么处理?
- 串口没有「包」的概念,只有字节流。Modbus RTU 靠 **3.5 个字符时间的静默间隔**分帧,帧内间隔不得超过 1.5 字符时间
- 但 Windows/Linux 的串口驱动 + USB 转换器有缓冲和调度延迟,**软件层面很难精确测出 3.5 字符时间**。工程上的做法:
  1. **按长度分帧**:Modbus 响应的第 3 个字节就是字节计数,读到它就知道还要收多少
  2. 配合超时兜底
- TCP:`Read` 返回值必须循环处理,自己实现 `ReadExactly`;Modbus TCP 有 MBAP 长度字段,先读 6 字节头再按 length 读剩余
- **实战补充**:我见过最坑的一次是 USB 转 485 的驱动把两帧粘在一起,靠 3.5T 判断完全失效,最后改成「先读固定头 + 按字节计数读体 + 校验 CRC,CRC 错就滑窗重新找帧头」。

### Q8. 超时时间怎么定?
- 下限 = 报文传输时间 + 设备处理时间。传输时间 = 总字节数 × 10 bit ÷ 波特率(8N1 一个字符 10 bit)
- 例:9600 波特、8 字节请求 + 25 字节响应 ≈ 34ms 纯传输,设备处理再给 50–100ms,超时设 300ms 比较稳
- **不能一刀切**:慢设备(老电表、无线透传)可能要 1–2 秒。所以超时应该是**每设备可配**
- 超时太短 → 误判掉线、重试风暴;太长 → 一台坏设备拖垮整条总线的轮询周期

### Q9. 设备掉线怎么快速发现,又不误判?
- **连续 N 次失败**才判离线(N=3),而不是一次失败就判;恢复时也要连续 M 次成功才判在线(去抖)
- 失败后**指数退避重连**:1s → 2s → 4s → 上限 30s,避免死设备把总线占满
- 离线设备移出正常轮询队列,进「慢速探活队列」,不影响其他设备周期
- MQTT 侧用 **LWT(遗嘱消息)**报告网关自身离线,检测延迟约 Keep Alive × 1.5
- **区分两层离线**:设备离线(485 不通)vs 网关离线(网络不通),看板上必须分开显示,否则运维会找错方向

### Q10. 轮询周期怎么定?会不会把总线跑满?
- 单条 485 总线是**半双工共享介质**,所有设备的轮询是串行的。总周期 = Σ(每台设备的请求+响应时间 + 帧间隔)
- 算例:9600 波特,10 台设备,每台一次往返约 40ms + 间隔 → 单轮约 500ms。想要 1 秒周期没问题,想要 100ms 周期就必须提速率、分总线或减点位
- 优化手段:①提高波特率(19200/115200)②批量读合并请求 ③按重要性分级(关键点位 1s,次要点位 10s)④拆多条总线并行
- **实战补充**:客户要求「所有点位 100ms 刷新」时,先算这笔账给他看,基本都会改需求。

### Q11. 断网续传具体怎么做?
- 采集数据先写本地 **SQLite**,带自增序号和 `sent` 标记
- 发送线程按序号批量读未发送数据 → MQTT 发布(QoS1)→ 收到 PUBACK 后标记已发送 → 定期清理
- **容量兜底**:磁盘也有上限,设最大保留条数/天数,超了丢最旧的(工业场景一般接受丢旧数据,不接受服务崩)
- **内存兜底**:采集和发送之间用有界 `Channel`,满了走落盘而不是堆内存
- **补传与实时混流**:补传数据打标记、走独立主题或降低速率,避免恢复瞬间把 Broker 和数据库打爆(我做了令牌桶限速)
- 时序库要能接受**乱序写入**(TDengine/InfluxDB 都可以),否则补传数据会被拒

### Q12. 时间戳用边缘时间还是平台时间?
- **以边缘采集时间为准**(数据的业务时间),同时记录平台接收时间(用于诊断延迟和补传)
- 边缘设备必须做时间同步:NTP;精度要求高的场合(如故障录波、时序对齐)用 PTP(IEEE 1588)
- 现场设备时钟不准是常态。我的处理:网关自己 NTP 对时,给数据打网关时间;若设备自带时间戳且明显漂移(和网关差超过阈值),记录告警但仍用网关时间
- **面试加分**:提一句「补传数据用采集时间入库,所以看板上会出现『过去时间点的新数据』,前端做实时曲线时要按时间排序而不是按到达顺序 append」

---

## 三、MQTT

### Q13. QoS 0/1/2 分别是什么语义,各有什么代价?
- **QoS0 至多一次**:发完就忘,可能丢。1 次报文交互。适合高频、可丢的遥测
- **QoS1 至少一次**:PUBLISH → PUBACK,可能**重复**。2 次交互。工业遥测的默认选择
- **QoS2 恰好一次**:PUBLISH → PUBREC → PUBREL → PUBCOMP,4 次交互,开销最大。只在「重复会造成业务错误」的场景用,比如**下发控制指令、计费数据**
- **实战判断**:遥测用 QoS1 + 业务侧去重,比用 QoS2 划算得多 —— QoS2 的吞吐能差一半以上。

### Q14. QoS1 会重复,业务侧怎么保证幂等?
三层配合:
1. 设备/网关侧给每条消息带**单调递增序号 + 设备 ID**
2. 平台侧维护**去重窗口**(比如按 deviceId + seq 做 LRU,或用 Redis SETNX 带 TTL)
3. 存储侧:时序库按 **(设备, 时间戳)** 做主键,重复写入直接覆盖成同值 —— 这是最省事的一层,天然幂等
- 注意:去重窗口要够大以覆盖断网续传的补发量,但也不能无限大

### Q15. Retain 用在哪?有什么坑?
- 用途:新订阅者立刻拿到**最后一条状态**,不用等下一次上报。适合「设备在线状态」「当前配置」这类**状态型**数据
- 坑一:retained 消息没有时效性,订阅者可能拿到几天前的旧值还当成实时数据 → **payload 里必须带时间戳**,消费方自己判断新鲜度
- 坑二:清除 retained 要发一条**空 payload + retain 标记**的消息,不是不发就自动没了
- 坑三:遥测数据不要开 retain,否则 Broker 会给每个主题都存一份,主题一多内存吃紧

### Q16. LWT(遗嘱)怎么用?
- 连接时注册遗嘱消息(主题 + payload + QoS + retain),客户端**异常断开**(网络断、崩溃、Keep Alive 超时)时由 Broker 代发
- 典型用法:遗嘱发 `{"online":false}` 到 `iiot/{device}/status` 并 retain;上线时自己发 `{"online":true}` retain
- 检测延迟 ≈ Keep Alive × 1.5。要更快就调小 Keep Alive,代价是心跳流量增加
- **注意**:客户端**正常** DISCONNECT 不会触发遗嘱,所以优雅退出时要自己发离线消息

### Q17. 共享订阅解决什么问题?
- 普通订阅是广播:每个订阅者都收到全量消息。后端要横向扩展时会重复处理
- 共享订阅 `$share/{group}/{topic}`:同组内的多个消费者**负载分担**,每条消息只投给组内一个
- MQTT 5 标准特性,EMQX 在 3.1.1 上也支持
- 用途:数据接入服务多实例部署、灰度、削峰

### Q18. 主题怎么设计?
- 原则:**从粗到细分层、语义清晰、便于通配订阅、不把易变信息放进主题**
- 我的规范:`iiot/{site}/{line}/{device}/telemetry | status | cmd | cmd_resp`
- 好处:`iiot/cd01/+/+/status` 一次订阅整个厂区状态;权限也能按主题前缀切
- 反例:把测点名放进主题(`.../temperature`)→ 点位一多主题爆炸,加点位要改订阅。**点位放 payload,设备放主题**
- 指令与响应分开主题,响应带 requestId 做关联

### Q19. MQTT 和 HTTP、CoAP 怎么选?
- **MQTT**:长连接、发布订阅、服务端可主动下发、开销小。设备数量多、需要下行控制 → 首选
- **HTTP**:请求响应、无状态、穿透性好、调试方便。低频上报、对接第三方系统 → 合适
- **CoAP**:UDP、极低开销,适合 NB-IoT 等窄带、电池设备
- **一句话总结**:「厂内设备接入用 MQTT,和 MES/ERP 这类业务系统对接用 HTTP,电池供电的窄带表计考虑 CoAP/LwM2M。」

---

## 四、OPC UA 与 PLC

### Q20. OPC UA 比 OPC DA 好在哪?
- DA 基于 **COM/DCOM**,只能跑 Windows,跨网段配 DCOM 是运维噩梦(防火墙、权限、超时)
- UA:**跨平台**(有 .NET / C / Java 实现)、传输走 opc.tcp(4840 端口,单端口易穿防火墙)、**内置安全**(证书、签名、加密)、有**信息模型**(不只是一堆平铺的 Tag,而是带类型、层次、语义的对象模型)
- 一句话:「DA 是 Windows 时代的接口,UA 是跨平台带语义和安全的架构。」

### Q21. OPC UA 的订阅机制说一下,Deadband 是什么?
- 客户端建 **Subscription**(有 PublishingInterval,决定服务器多久推一次)
- 在 Subscription 下建 **MonitoredItem**(有 SamplingInterval,决定服务器多快采一次;QueueSize 与丢弃策略决定采样快于推送时怎么存)
- **DataChangeFilter** 决定「什么算变化」:仅状态变 / 值变 / 值或时间戳变
- **Deadband(死区)**:绝对死区(变化小于 X 才不上报)或百分比死区(按量程百分比)。用来**过滤模拟量的抖动**,大幅降低无效流量
- 面试常见追问「订阅比轮询好在哪」:服务器侧采样、只推变化、一次连接管多个点位,网络往返和带宽都省

### Q22. OPC UA 的安全怎么配?现场为什么老连不上?
- 两个维度:**安全策略**(None / Basic256Sha256 / Aes128_Sha256_RsaOaep;Basic128Rsa15 和 Basic256 已废弃不安全)和**消息安全模式**(None / Sign / SignAndEncrypt)
- 连不上最常见的原因:**证书互信没配**。客户端第一次连接会被服务端拒绝并把证书丢进 `rejected/certs`,要手动移到 `trusted/certs` 才行(反之亦然)
- 其他坑:主机名/IP 与证书里的 ApplicationUri 不匹配、证书过期、时间不同步导致证书校验失败、端点 URL 里的主机名内网解析不了
- **实战话术**:「现场我一般先用 None 策略连通验证链路,再逐步开安全,避免同时排查通信和证书两件事。」

### Q23. 西门子 S7 读 DB 块,优化块是什么坑?
- S7-1200/1500 默认开启「**优化的块访问**」,此时 DB 里的变量**没有固定的绝对地址**,`DB1.DBD0` 这种寻址读不到
- 两个解法:①在 TIA Portal 里对该 DB 右键属性,**取消勾选「优化的块访问」**;②用支持符号访问的方式(OPC UA 服务器、或 S7 的符号接口)
- 还要在 CPU 属性 → 防护与安全里勾上「**允许来自远程伙伴的 PUT/GET 通信访问**」,否则 S7.Net 直接连不上
- 连接参数:走 **ISO-on-TCP(RFC1006),端口 102**,需要机架号和槽号(S7-1200/1500 通常 Rack 0 / Slot 1;S7-300 常见 Rack 0 / Slot 2)
- **字节序:PLC 是大端**,读出来要转换

### Q24. 为什么现场常见「OPC UA 采集 → MQTT 上云」?
- **OPC UA 强在厂内**:信息模型丰富、语义完整、安全性好、和 PLC/SCADA 生态天然对接。但它是**会话式、偏重**,穿越防火墙上公网、面对不稳定网络、面对海量设备时不划算
- **MQTT 强在上云**:轻量、长连接、发布订阅、断线重连成熟、Broker 水平扩展容易、一个出口连接即可(符合工厂只开一个出网口的安全策略)
- 所以典型分层是:**PLC ──OPC UA──> 边缘网关 ──MQTT──> 云平台**。网关同时承担协议转换、缓存续传、边缘计算和安全隔离
- 补充一句:OPC UA 也有 **PubSub 规范**(可跑在 UDP 组播或 MQTT 上),方向上在收敛,但现场落地目前仍以「UA 采 + MQTT 传」为主。

---

## 五、架构与规模

### Q25. 百万点位怎么存?
- 用**时序数据库**,不用关系库。TDengine 的「一设备一子表 + 超级表 + 标签」模型,或 InfluxDB 的 measurement + tag
- 算账先行(面试时当场算给他看):
  - 500 设备 × 20 点位 × 1s = **8.64 亿行/天**
  - 每行约 20 字节 → 原始约 17 GB/天,按 10:1 压缩 ≈ **1.7 GB/天** → 一年约 620 GB
- 所以必须**分级保留**:原始数据 30 天,1 分钟聚合留 1 年,1 小时聚合留 3 年。用连续查询/流计算自动降采样
- 写入侧:**批量 + 参数绑定**,不要单条 insert;吞吐差一个数量级
- 查询侧:看板只查聚合表,不要拿原始表做一年跨度的查询

### Q26. 你自己做的这套,和直接买组态软件 / 用 ThingsBoard 有什么区别?为什么不直接用?
(这题是考**判断力**,不是考技术)
- 组态软件(KingSCADA、力控、WinCC)成熟稳定,但**授权按点数收费**、二次开发受限、跨平台差、上云能力弱
- ThingsBoard 等开源平台功能全,但**工业协议接入弱**(强在 MQTT/HTTP,Modbus/S7/OPC UA 要自己补网关)、规则链做复杂业务不如写代码、深度定制要读 Java 源码
- **诚实的答案**:「标准场景我倾向于用成熟产品,自研只在两种情况下划算 —— 一是协议适配和现场逻辑高度定制,二是点数规模大到授权费超过开发成本。我做这个项目是为了打通整条链路、理解每一层的取舍,真实项目里我会先评估买还是做。」
- ⚠️ **不要说「因为开源的都不好用所以我全自研」**,这会被判定为不成熟。

### Q27. 边缘计算在这套系统里做什么?
- **降流量**:死区过滤、变化上传、边缘聚合(1 秒采、1 分钟传均值/极值)
- **降延迟**:本地实时报警与联锁,不依赖云端往返
- **保可用**:断网时本地照常采集、报警、缓存,恢复后续传
- **保安全/合规**:敏感数据本地处理,只上传聚合结果
- 一句话:「云负责长周期分析和跨厂对比,边缘负责实时性和可用性。」

### Q28. 系统怎么保证不丢数据?讲一条数据的完整生命周期。
1. 驱动采集 → 带质量码(好/坏/不确定)和采集时间戳
2. 写本地 SQLite(事务提交后才算落地)
3. 发送线程读未发送 → MQTT QoS1 发布
4. 收到 PUBACK → 标记已发送
5. 平台侧订阅 → 去重 → 批量写 TDengine((设备,时间戳) 主键天然幂等)
6. 写入成功后再 ACK 上游(如果用了确认机制)
- **薄弱点主动说出来**:「最脆弱的是第 2 步之前 —— 采到但还没落盘时断电会丢这一个周期的数据。要消除就得同步落盘,代价是吞吐。工业场景一般接受丢 1 个采集周期。」
- 主动指出系统的边界和取舍,比声称「绝对不丢」可信得多。

---

## 六、现场问题(最能区分「做过项目」和「看过教程」)

### Q29. 客户不让改 PLC 程序,怎么采数?
- 首选**只读采集**:S7 的 PUT/GET 读 DB(不写)、Modbus TCP 从站功能、PLC 自带 OPC UA Server(S7-1500 内置)
- 如果 PLC 没开通信口:加**协议转换网关**、或从 HMI/SCADA 侧的 OPC Server 取数、或从现有 MES 数据库取
- 实在不行:**外挂传感器**(电流互感器判断设备启停、光电传感器数产量),完全不碰 PLC
- **话术**:「先摸清客户为什么不让改 —— 通常是怕影响生产和责任划分。我会给出只读方案和停机窗口内的验证计划,先降低他的风险感知。」

### Q30. 现场通信不稳定,怎么定位?
按层排查,从物理层往上:
1. **物理层**:接线极性(A/B 反接)、终端电阻数量、屏蔽层接地(单端!)、线缆是否和动力线走同一桥架、线径与距离
2. **参数层**:波特率、校验位、停止位、从站地址是否冲突
3. **协议层**:抓包看是全无响应还是 CRC 错。全无响应 → 接线/地址/参数;**CRC 错 → 典型的干扰或阻抗问题**
4. **软件层**:超时是否太短、轮询是否太密、多线程抢占同一串口
- 常用手段:降低波特率试(干扰环境下 9600 比 115200 稳)、换隔离型转换器、加中继器、示波器看差分波形
- **实战故事准备**:准备一个自己真实排查过的案例,讲清「现象 → 假设 → 验证 → 结论」。这比背十条理论有用。

### Q31. 现场没有网络怎么调试?
- 笔记本直连:网线直连 PLC / 交换机,手动配同网段静态 IP
- 手机热点给树莓派做临时出网(装包、拉代码)
- **提前准备离线包**:.NET Runtime、NuGet 包、Docker 镜像 `docker save` 打包带走
- 部署脚本要能**完全离线执行**,不能装到一半去联网拉依赖
- 带齐工具:USB 转 485、网线、串口调试助手、Modbus Poll、Wireshark、UaExpert 都装在本机

### Q32. 上线后客户说数据不对,怎么办?
- 先**分层定位**:看板不对 → 查数据库 → 查 MQTT 报文 → 查驱动解析 → 查设备原始寄存器。一层层往下,不要跳
- 保留**原始报文日志**(可开关的调试级日志),这是自证清白的关键证据
- 常见原因排序:①字序/缩放系数配错 ②地址差 1 ③设备本身就不准(传感器故障/未校准)④时区问题(UTC vs 本地时间)⑤补传数据造成的曲线错位
- **态度上的加分**:「我会先和客户一起看设备本地显示值,确认基准。争论谁对谁错没意义,先把参照物定下来。」

---

## 七、给 10 年经验者的必答题

### Q33. 你做了 10 年 C#,为什么转物联网?
不要说「原来的方向不行了」「想涨薪」。
建议叙事:「我早年做过串口和 Modbus 的设备采集,一直对**软件直接驱动物理世界**这件事更有兴趣。这几年做业务系统,离设备越来越远。所以我系统性地把这块补齐了 —— OPC UA、PLC、边缘网关、时序库,并且自己搭了一套完整的数采系统跑在真实设备上。这是我想长期做下去的方向。」
—— 关键是**有连续性**(不是转行,是回归 + 深化)和**有证据**(项目 + 实物)。

### Q34. 你的项目是自己练手的,和真实生产环境差在哪?
主动、诚实地答,这题答好非常加分:
「差在三点:①**规模**,我压测到 200 台设备,真实产线可能上千,分库分表和多网关协同我只做了设计没验证;②**现场复杂度**,我的设备是干净的实验环境,没有真实车间的电磁干扰、老旧设备的私有协议、和跨厂区网络限制;③**运维长尾**,7×24 跑几年会遇到什么问题我没经历过。
所以我做项目时重点保证的是**架构和取舍是对的**,规模和长尾我需要在真实项目里补。」

### Q35. 让你带一个 3 人小组做这套系统,你怎么分工和排期?
考的是**技术骨干视角**,准备好:
- 分工:1 人边缘(驱动 + 网关,最难,自己盯)、1 人平台(接入 + 存储 + 告警)、1 人前端(上位机 + 看板)
- 排期:先打通**最小可用链路**(一个 Modbus 设备 → MQTT → 库 → 一张图),两周内出可演示版本,再横向铺协议、纵向加功能
- 关键约定先定:**点表格式、MQTT 主题规范、数据模型**。这三样定不下来,三个人会各写各的
- 风险前置:现场协议不确定 → 第一周就去要设备手册和试连;硬件采购周期长 → 立刻下单
- 一句话:「先通链路再铺宽度,先定接口再并行。」

---

## 八、你要反问的问题(面试是双向的)

- 这个岗位的数采部分是自研还是基于成熟平台二次开发?
- 团队现在的技术栈里,协议接入这块是谁在维护?
- 出差比例大概多少?是安装调试期集中出差,还是长期驻场?
- 项目是产品化的还是项目制的?一年大概交付几个?
- 现有系统最大的技术债在哪?
- 这个岗位期望我半年内解决的最重要的一个问题是什么?
