# 半年で産業用 IoT へ転向するロードマップ · 成都 月給 15,000〜20,000 元

> 開始:2026-09 · 目標達成:2027-03
> 背景:C# 開発歴 10 年。数年前にシリアル / TCP / Modbus によるデータ収集を経験(現在は忘れかけている)
> ペース:在職中、週 10〜15 時間(半年で約 280〜360 時間)

---

## 0. 戦略判断

### なぜ産業用 IoT なのか

| 方向性 | 既存経験の再利用率 | 半年で 15,000〜20,000 元に届く可能性 |
|---|---|---|
| **産業用 IoT / データ収集・上位機ソフト** | **80% 以上**(C#、シリアル、Modbus、TCP がそのまま活きる) | **高い** |
| IoT クラウドバックエンド(Java/Go) | 30%(汎用的な設計力しか残らない) | 低い — 言語エコシステムと分散システムを同時に習得したうえで、情報系出身の Java 技術者と競うことになる |
| 組込みファームウェア(C/RTOS) | 10% | 極めて低い — 実質ゼロからのやり直し |
| コンシューマ向けスマートハードウェア | 40% | 中〜低 — 求人は深圳に集中し、成都では少ない |

半年で到達できるのは、**一つの方向**において「使える」から「一人で任せられる」までです。ならば再利用率が最も高い道を選ぶべきです。

### 成都の給与水準(率直に)

- **12,000〜15,000 元**:上位機ソフトが書け、Modbus が分かり、プロジェクトに参加できる水準。今の下地なら 1 か月の復習で到達します。
- **15,000〜20,000 元(狙う的)**:データ収集システムを選定から導入まで一人で担当でき、PLC と OPC UA を理解し、客先での現地調整ができ、アーキテクチャ上の取捨選択を筋道立てて説明できる水準。
- **20,000 元以上**:業界固有の深さ(半導体・新エネルギー・防衛)またはチームを率いた経験が必要。半年では届きません。

⚠️ **10 年の経験は諸刃の剣です。** 面接官は「技術リーダー」の基準であなたを見ます。実装の詳細を答えるだけでは通りません。アーキテクチャ、踏んだ落とし穴、なぜ A を選び B を選ばなかったのかを語れることが必須です。これはフレームワークをもう一つ覚えることより、はるかに重要です。

**代替ルート**:半年で届かなければ、まず 12,000〜15,000 元の職に就き、1 年の実務を積んでから動く。それでも Java や組込みへの転向より総所要時間は短く済みます。

---

## 1. 技術スタックの範囲

### 必修(競争力の核)

**プロトコル層** — ここが「CRUD を書く C# 技術者」との分かれ目です
- Modbus RTU / TCP — バイト単位で理解する。「ライブラリを呼べる」では足りない
- Siemens S7comm — DB ブロックの読み書き、最適化ブロックと非最適化ブロックの違い
- **OPC UA** — アドレス空間、サブスクリプション、証明書によるセキュリティ。産業界の相互接続における事実上の標準
- MQTT 3.1.1 / 5 — QoS のセマンティクス、トピック設計、共有サブスクリプション

**エッジ層**
- Linux:systemd サービス、udev ルールによるシリアルデバイス名の固定、`ip` / `ss` / `tcpdump` による切り分け
- Docker / docker compose
- .NET 8:Worker Service、`linux-arm64` 向け発行、`System.Threading.Channels`、Polly、Serilog
- SQLite によるローカルバッファ → 通信断からの再送

**プラットフォーム層**
- TDengine(中国の産業分野における時系列 DB の定番)/ InfluxDB
- Grafana によるダッシュボードとアラート
- EMQX(MQTT ブローカー)
- ThingsBoard(モノのモデル / ルールチェーン / デバイスシャドウという抽象を体得するため)
- Alibaba Cloud IoT または Huawei Cloud IoTDA — **どちらか一つ**を選び、通しで動かす

**上位機・アプリケーション層**
- WPF + CommunityToolkit.Mvvm(産業用 HMI の主流)
- ScottPlot / LiveCharts2 によるリアルタイムグラフ(大量データの描画)
- SignalR によるリアルタイム配信
- Blazor Server による Web 大画面(任意)

**業界知識** — インテグレーターでもユーザー企業でも必ず問われます
- ISA-95 の階層:L0 現場機器 → L1 制御 → L2 監視(SCADA) → L3 製造実行(MES) → L4 基幹(ERP)
- PLC / HMI / SCADA / DCS / MES それぞれの役割と境界
- OEE、稼働率、タクトタイム、作業指示、トレーサビリティ
- IEC 62443 のゾーンとコンジット(概念を説明できれば十分)

### 明確に扱わないもの(半年では投資対効果が負)

C 言語のファームウェア、RTOS、基板設計、LoRa/NB-IoT の物理層、Kubernetes の深部、Java/Go のエコシステム、Kafka の内部実装。

> 面接で**位置づけと適用場面を説明できれば十分**で、手を動かす必要はありません。
> 例:「NB-IoT は低頻度送信・電池駆動・広域カバーに向きます。水道メーターやガスメーターですね。工場内であれば有線と WiFi で足りるので、通信事業者の網を持ち込む理由はありません。」

---

## 2. 半年の計画

### M1 · 第 1〜4 週:プロトコルの復習と Modbus の徹底

| 週 | 内容 |
|---|---|
| **W1** | ハードウェアの組み立て。RS-232 と RS-485 の電気的な違い、終端抵抗(120Ω、両端のみ)、シールドの一点接地、コモングランドの問題。C# `SerialPort` の落とし穴:`ReadTimeout`、`DataReceived` イベントの分割受信、`BytesToRead` とバッファ。TCP の分割受信とフレーミング。 |
| **W2** | Modbus 仕様をバイト単位で読み解く:ファンクションコード 01/02/03/04/05/06/0F/10、CRC16 アルゴリズム(テーブル方式)、例外応答(ファンクションコード + 0x80)。Modbus スレーブシミュレーターと Wireshark のキャプチャを一バイトずつ突き合わせる。 |
| **W3** | **Modbus ライブラリを自作する**(まずは NModbus を使わない):RTU / TCP 両対応、タイムアウトとリトライ、離散したアドレスを**最少のリクエストにまとめる**一括読み出し最適化、32 ビット浮動小数点のワード順(ABCD / CDAB / BADC / DCBA)を設定可能に。 |
| **W4** | 複数機器のポーリングスケジューラ:優先度キュー、遅い機器の隔離、指数バックオフによる再接続、通信品質の統計(成功率 / 平均遅延 / タイムアウト回数)。実際の RS-485 バスにスレーブを 2〜3 台つなぐ。 |

**成果物**
- `Iiot.Drivers.Modbus` ライブラリと単体テスト(CRC、ワード順、フレームのエンコード / デコード)
- 自作の「Modbus 現場トラブルシューティング手引き」— これが面接での話材になります

**関門チェック**:資料を閉じた状態で、ファンクションコード 03 の要求・応答のバイト構成を紙に書き、CRC が下位バイト先行で送られる理由を説明できること。

---

### M2 · 第 5〜8 週:MQTT と Linux エッジゲートウェイ

| 週 | 内容 |
|---|---|
| **W5** | Raspberry Pi に Ubuntu Server を導入。.NET 8 を `linux-arm64` の単一ファイルとして発行し、systemd サービス化(起動時自動実行、クラッシュ時再起動)。udev ルールで USB シリアル変換器をシリアル番号により固定(`/dev/ttyModbus0`)。`ip a`、`ss -tunlp`、`tcpdump -i eth0 port 502` による切り分け。 |
| **W6** | MQTT の原理:QoS 0/1/2 の意味とコスト、Retain、LWT(遺言メッセージによるオフライン検知)、Clean Session / Session Expiry、共有サブスクリプション(`$share/group/topic`)。Docker で EMQX を構築。MQTTnet クライアント。**トピック設計の規約**(`iiot/{site}/{line}/{device}/telemetry`)。 |
| **W7** | M1 のドライバーをエッジサービス化:収集 → SQLite にバッファ → MQTT で送信 → **通信断からの自動再送** → ACK 受領後に削除。バックプレッシャーとメモリ上限(有界 `Channel` と破棄ポリシー)。 |
| **W8** | 堅牢化:Polly によるリトライとサーキットブレーカー、ヘルスチェック、グレースフルシャットダウン(`IHostApplicationLifetime`)、設定のホットリロード(`IOptionsMonitor`)、Serilog による構造化ログのローテーション、MQTT 経由のリモート設定配信。 |

**成果物**
- Raspberry Pi 上で 24 時間 365 日安定稼働する `Iiot.EdgeGateway`
- **LAN ケーブルを抜いて再送を実演した画面録画。** 面接でこれを流すほうが、言葉で十回説明するより効きます。

**関門チェック**:QoS 1 では重複配信が起こりうること、業務側でどう冪等性を担保するか(メッセージ ID + 機器側の単調増加シーケンス番号 + プラットフォーム側の重複排除ウィンドウ)を説明できること。

---

### M3 · 第 9〜12 週:PLC と OPC UA(**決定的な分水嶺**)

| 週 | 内容 |
|---|---|
| **W9** | PLC 入門:配線(24V、入出力のコモン端子)、プログラミングソフトの導入、ラダー図を読む、DB ブロック、データ型とバイト順(**PLC はビッグエンディアン**)。起動 / 停止 + カウンタ + タイマのロジックを自分で書く。 |
| **W10** | S7.Net Plus / Sharp7 による DB ブロックの読み書き。**最適化ブロックと非最適化ブロック**(最適化ブロックには絶対アドレスがないため、最適化を解除するかシンボリックアクセスを使う)、絶対アドレスの計算(`DB1.DBD0`)。上位機のボタンで PLC の出力を操作し、PLC のカウンタ値を上位機に返す、という結合試験まで。 |
| **W11** | OPC UA の深掘り:UaExpert でアドレス空間を閲覧、NodeId の 4 種類、Subscription / MonitoredItem / PublishingInterval / SamplingInterval / **デッドバンド**、キューと破棄ポリシー、証明書の信頼設定(クライアント証明書をサーバーの `trusted/certs` に配置)、セキュリティポリシー(None / Basic256Sha256)、ユーザートークン。OPC Foundation の `UA-.NETStandard` でクライアントを実装。 |
| **W12** | **OPC UA サーバーを自作**し、M1 で収集した Modbus のデータを情報モデルとして公開する。OPC UA PubSub / over MQTT の位置づけを理解する。 |

**成果物**
- `Iiot.Drivers.S7` と `Iiot.Drivers.OpcUa`。Modbus と併せて `IDeviceDriver` という統一抽象にまとめる
- 実演できる実機デモ台(PLC + 変換器 + Raspberry Pi)

**関門チェック**:OPC UA と MQTT の適用場面を対比して説明し、現場で「**OPC UA で収集 → MQTT でクラウドへ**」という構成が多い理由を語れること。

> ⚠️ **この 1 か月が 15,000 元と 20,000 元の境目です。** 遅れが出たら、この月を守り、M4 の ThingsBoard とクラウドを削ってください。

---

### M4 · 第 13〜16 週:データ基盤と可視化

| 週 | 内容 |
|---|---|
| **W13** | TDengine の構築とモデリング:スーパーテーブル / サブテーブル / タグの設計、1 機器 1 サブテーブル、書き込み性能の測定、ダウンサンプリング(`INTERVAL`)、保持期間(`KEEP`)。InfluxDB との比較(エコシステム、日本語 / 中国語資料、SQL の扱いやすさ)。 |
| **W14** | Grafana から TDengine を参照してリアルタイムダッシュボードとアラートを構築。テンプレート変数による機器切り替え。圧縮とホット / コールド階層化。 |
| **W15** | ThingsBoard の構築:デバイス登録 → モノのモデル → ルールチェーン → ダッシュボード。**属性 / テレメトリ / RPC / デバイスシャドウ**という抽象を身につける — あらゆる IoT プラットフォームに共通する語彙です。 |
| **W16** | Alibaba Cloud IoT か Huawei Cloud IoTDA の**どちらか一つ**を通しで動かす:モノのモデル定義、デバイスシャドウ、OTA、ルールエンジンから自作 HTTP サービスへの転送。無料枠で十分です。 |

**成果物**:`docker-compose.yml`(EMQX + TDengine + Grafana)一発起動と、端から端まで通ったデータ経路

**関門チェック**:**500 台 × 20 点 × 1 秒周期**を捌けるストレージ設計を示し、その場で日次行数、生データ量、圧縮後の量、1 年間のディスク使用量を算出できること。
> 計算例:500 × 20 × 86400 = 8 億 6,400 万行/日。スーパーテーブル設計で 1 行約 20 バイト、圧縮比 10:1 とすると約 1.7 GB/日 → 年間およそ 620 GB。結論として、ダウンサンプリングと段階的な保持(生データ 30 日、分単位 1 年)は必須です。

---

### M5 · 第 17〜20 週:統合プロジェクト(職務経歴書の中核)

**プロジェクト名:工場設備データ収集・監視システム**

```
現場機器 (Modbus RTU/TCP · Siemens S7 · OPC UA)
   │  統一ドライバー抽象 IDeviceDriver + 点数表設定(Excel / JSON 取り込み)
   ▼
エッジゲートウェイ (Raspberry Pi · .NET 8)
   収集スケジューラ → SQLite バッファ → 通信断からの再送 → 通信品質統計
   │  MQTT (EMQX) + モノのモデル JSON
   ▼
取り込みサービス (.NET) → TDengine
   └─ アラームエンジン(閾値 / 継続時間 / 重要度 / チャタリング防止 / 復旧通知)
   │
   ├─ WPF 上位機(リアルタイムグラフ · 点数監視 · 手動書き込み · アラーム一覧 · 履歴照会)
   └─ Grafana 大画面
```

**内定を左右する作り込み** — 面接官は必ずここを掘ります
- OEE の算出(時間稼働率 × 性能稼働率 × 良品率)
- 機器ごとの稼働率と通信品質の統計(どの機器が取りこぼしているか一目で分かる)
- 点数表のホットリロード(収集を止めずに設定変更)
- **ワンクリックで 200 台をシミュレート**する負荷試験 — 規模を考えている証拠になります
- タイムスタンプの方針:エッジ時刻とプラットフォーム時刻、現場機器の時計がずれている場合の扱い

| 週 | 内容 |
|---|---|
| W17〜18 | エッジ層とプラットフォーム層を通す |
| W19 | WPF 上位機の UI |
| W20 | 負荷試験、アーキテクチャ図、README、デプロイスクリプト、**3〜5 分のデモ動画** |

---

### M6 · 第 21〜24 週:就職活動

| 週 | 内容 |
|---|---|
| **W21** | 職務経歴書の書き直し。10 年の C# を「**産業用ソフトウェア / 機器接続**」という物語に組み替える。各職歴を STAR 形式と定量的な数字に。古い下地(シリアルと Modbus の実務)と新しい成果(完成したデータ収集システム)の両方を前面に。 |
| **W22** | 想定問答を声に出して練習(`interview-qa.md` を参照)。**ホワイトボードにアーキテクチャを描く**練習 — 経験 10 年なら図を求められる確率はほぼ 100% です。 |
| **W23** | 応募。各カテゴリ 15〜20 社。**第二志望群から練習として応募し**、1 週間後に本命へ。 |
| **W24** | 振り返りと改善、そして条件交渉(アンカリング、13 / 14 か月分、等級、出張手当、昇給サイクル)。 |

**成都における応募先の分類**
1. **産業用ソフトウェア / 自動化インテグレーター** — 上位機、SCADA、MES、データ収集基盤。C# の求人が最も多い一方、出張が多くなりがち。
2. **メーカーの社内スマート製造 / IT 部門** — 電子、装置、新エネルギー、防衛系研究所。安定して出張も少ないが、業務理解とシステム導入の実績をより重く見る。
3. **エネルギー・都市インフラ** — 電力、水道、ガス。Modbus / DL-T645 などの規約案件が多く、C# の既存システムも多い。あなたの旧来の経験がそのまま活きます。
4. **IoT プラットフォーム製品企業** — 補完として応募。

---

## 3. 学習リソース

**規格と公式ドキュメント**(一次情報を最優先)
- Modbus 規格:`Modbus_Application_Protocol_V1_1b3.pdf`、`Modbus_over_serial_line_V1_02.pdf`(modbus.org から無償)
- OPC Foundation `UA-.NETStandard` の GitHub リポジトリ、`Samples/` ディレクトリ
- MQTTnet Wiki、EMQX ドキュメント、TDengine 公式ドキュメント
- HiveMQ の *MQTT Essentials* 連載 — QoS の説明として最も明快

**書籍**
- 『OPC Unified Architecture』(Mahnke / Leitner / Damm)— OPC UA の定番。最初の 4 章で十分
- 購入した PLC のプログラミングマニュアル(Siemens S7-1200 または国産機)
- 『データ指向アプリケーションデザイン』第 11 章(ストリーム処理)— 時系列データと配信保証の理解に

**コミュニティ**
- 中国語圏の C# 上位機開発コミュニティ(この分野では cnblogs が最も活発)
- bilibili:Siemens 公式チャンネル、上位機開発の講座
- 知乎の「産業自動化」「産業インターネット」トピック

---

## 4. 検証方法:毎月末の 30 分セルフチェック

基準は「**ホワイトボードの前で説明できるか**」であり、「読んだかどうか」ではありません。

| 時点 | 合格基準 |
|---|---|
| M1 末 | Modbus FC03 のフレームを手書きし、全バイトを説明できる。実際の RS-485 バス上で 2 台から収集できている |
| M2 末 | Raspberry Pi 上のサービスが 72 時間無停止。ケーブルを 10 分抜いた後、欠損ゼロで再送が完了する |
| M3 末 | 自作の OPC UA サーバーを UaExpert で閲覧できる。S7 の DB 読み書きが成功する。OPC UA と MQTT の違いを語れる |
| M4 末 | docker-compose 一発で全スタックが起動。Grafana がリアルタイム更新。ストレージ容量をその場で見積もれる |
| M5 末 | README、アーキテクチャ図、デモ動画が完成。200 台の負荷試験に合格 |
| M6 末 | 技術面接を最低 3 社通しで経験。内定、または不足点の具体的なリストを得ている |

---

## 5. リスクと対応

| リスク | 対応 |
|---|---|
| 業務に押されて週 8 時間しか取れない | 最小構成:M1 / M2 / M3 / M5 のみ残し、M4 の ThingsBoard とクラウドを削除。期間は 7〜8 か月へ延長。 |
| M3(PLC + OPC UA)が遅れる | ただちに M4 から 2 週間を回す。**PLC と OPC UA は譲れません** — 15,000 元と 20,000 元の分かれ目です。 |
| ハードウェアの到着が遅い | 第 1〜2 週は Modbus スレーブシミュレーターと PLCSIM で進める。待って手を止めないこと。 |
| 学べるが説明できない | M1 から毎月 1 本、技術記事を公開する。書くことは話すことの最良の予行演習であり、そのままポートフォリオになります。 |
| 半年後も 15,000 元に届かない | 12,000〜15,000 元の職に就き、1 年の実務を経て再び動く。**2,000 元の差のために足踏みしないこと。** |
