# 半年转型工业物联网(IIoT)路线图 · 成都 15–20k

> 起始:2026-09 · 目标达成:2027-03
> 背景:10 年 C# 开发,数年前做过串口 / TCP / Modbus 采集(已生疏)
> 节奏:在职,每周 10–15 小时(半年约 280–360 小时)

---

## 0. 战略判断

### 为什么选工业物联网,而不是别的方向

| 方向 | 存量经验复用率 | 半年到 15–20k 的可行性 |
|---|---|---|
| **工业物联网 / 数采上位机** | **80%+**(C#、串口、Modbus、TCP 全部直接可用) | **高** |
| IoT 云平台后端(Java/Go) | 30%(只剩通用工程能力) | 低 — 要同时补语言生态和分布式,还得跟科班 Java 竞争 |
| 嵌入式固件(C/RTOS) | 10% | 极低 — 等于从头开始 |
| 消费级智能硬件 | 40% | 中低 — 岗位集中在深圳,成都机会少 |

半年只够在**一个方向**上从"会用"走到"能独立负责"。所以必须选复用率最高的那条。

### 成都薪资锚点(诚实版)

- **12–15k**:能写上位机、会 Modbus、能跟着做项目。以你现在的底子,复健一个月就够。
- **15–20k(靶心)**:能独立负责一套数采系统从选型到落地 + 懂 PLC 与 OPC UA + 能出差现场调试 + 讲得清架构取舍。
- **20k+**:再加行业深度(半导体 / 新能源 / 军工)或带团队经验,不是半年的事。

⚠️ **10 年经验是双刃剑**:面试官会按"技术骨干"标准考你。只答实现细节不够,必须能讲架构、讲踩过的坑、讲为什么这么选而不是那么选。这一点比多学一个框架重要得多。

**后备路径**:若 6 个月未达标,先接 12–15k 的岗位,用 1 年真实项目经验再跳。总耗时仍然远优于转 Java 或嵌入式。

---

## 1. 技术栈范围

### 必学(核心竞争力)

**协议层**(这是你和"会写 CRUD 的 C# 程序员"的分水岭)
- Modbus RTU / TCP — 字节级手撕,不是"会调库"
- 西门子 S7comm — DB 块读写、优化块与非优化块
- **OPC UA** — 地址空间、订阅、证书安全。工业互联互通的事实标准
- MQTT 3.1.1 / 5 — QoS 语义、主题设计、共享订阅

**边缘层**
- Linux:systemd 服务、udev 固定串口名、`ip` / `ss` / `tcpdump` 排障
- Docker / docker compose
- .NET 8:Worker Service、`linux-arm64` 发布、`System.Threading.Channels`、Polly、Serilog
- SQLite 本地缓冲 → 断网续传

**平台层**
- TDengine(国内工业时序库首选)/ InfluxDB
- Grafana 看板与告警
- EMQX(MQTT Broker)
- ThingsBoard(理解物模型 / 规则链 / 设备影子的抽象)
- 阿里云 IoT 或华为云 IoTDA(**二选一**,跑通即可)

**上位机层**
- WPF + CommunityToolkit.Mvvm(工业上位机主流)
- ScottPlot / LiveCharts2 实时曲线(大数据量渲染)
- SignalR 实时推送
- Blazor Server 做 Web 大屏(可选)

**行业知识**(甲方和集成商必问)
- ISA-95 分层:L0 现场设备 → L1 控制 → L2 监控(SCADA) → L3 制造运行(MES) → L4 企业(ERP)
- PLC / HMI / SCADA / DCS / MES 各自的定位与边界
- OEE、稼动率、节拍(Takt Time)、工单、追溯
- IEC 62443 工控安全分区分域(说得清概念即可)

### 明确不学(半年内投入产出比为负)

C 语言固件、RTOS、PCB 设计、LoRa/NB-IoT 物理层、Kubernetes 深水区、Java/Go 生态、Kafka 源码。

> 这些只需要在面试里**说清定位和适用场景**,不需要动手。
> 例:"NB-IoT 适合低频上报、电池供电、广域覆盖的场景,比如水表气表;我们车间内部用有线 + WiFi 就够了,不需要引入运营商网络。"

---

## 2. 六个月计划

### M1 · 第 1–4 周:协议复健 + Modbus 打透

| 周 | 内容 |
|---|---|
| **W1** | 硬件到货组装。RS-232 / RS-485 电气差异、终端电阻(120Ω,只装两端)、屏蔽层单端接地、共地问题。C# `SerialPort` 的坑:`ReadTimeout`、`DataReceived` 事件分包、`BytesToRead` 与缓冲区。Socket 半包/粘包。 |
| **W2** | Modbus 规范逐字节手撕:功能码 01/02/03/04/05/06/0F/10、CRC16 算法(查表法)、异常响应(功能码 +0x80)。用 Modbus Slave 模拟器 + Wireshark 抓 TCP 报文逐字节对照。 |
| **W3** | **自己实现 Modbus 库**(先别用 NModbus):RTU / TCP 双模式、超时重试、离散地址**合并成最少请求**的批量读优化、32 位浮点字序(ABCD / CDAB / BADC / DCBA)可配置。 |
| **W4** | 多设备轮询调度器:优先级队列、慢设备隔离、掉线指数退避重连、通信质量统计(成功率 / 平均时延 / 超时次数)。真实 485 总线挂 2–3 个从站。 |

**产出**
- `Iiot.Drivers.Modbus` 库 + 单元测试(CRC、字序、报文编解码)
- 《Modbus 现场排错手册》(自己写,这是面试谈资)

**闸门自测**:合上文档,在纸上写出 03 功能码的请求与响应字节结构,并解释 CRC 为什么低字节在前。

---

### M2 · 第 5–8 周:MQTT + Linux 边缘网关

| 周 | 内容 |
|---|---|
| **W5** | 树莓派装 Ubuntu Server。.NET 8 发布为 `linux-arm64` 单文件 + systemd 服务(开机自启、崩溃重启)。udev 规则按 USB 串口序列号固定设备名(`/dev/ttyModbus0`)。`ip a` / `ss -tunlp` / `tcpdump -i eth0 port 502` 排障。 |
| **W6** | MQTT 原理:QoS 0/1/2 的语义与代价、Retain、LWT(遗嘱做设备离线检测)、Clean Session / Session Expiry、共享订阅(`$share/group/topic`)。Docker 部署 EMQX。MQTTnet 客户端。**主题设计规范**(`iiot/{site}/{line}/{device}/telemetry`)。 |
| **W7** | 把 M1 驱动做成边缘服务:采集 → SQLite 本地缓冲 → MQTT 上报 → **断网自动续传** → 收到 ACK 后清理。背压控制与内存上限(`Channel` 有界队列 + 丢弃策略)。 |
| **W8** | 可靠性打磨:Polly 重试与熔断、健康检查端点、优雅退出(`IHostApplicationLifetime`)、配置热加载(`IOptionsMonitor`)、Serilog 结构化日志滚动、通过 MQTT 下发远程配置。 |

**产出**
- 树莓派上 7×24 稳定运行的 `Iiot.EdgeGateway`
- **拔网线演示断网续传的录屏**(面试时直接放,比说十句都有用)

**闸门自测**:讲清 QoS1 会重复投递,业务侧怎么做幂等(消息 ID + 设备侧单调递增序号 + 平台侧去重窗口)。

---

### M3 · 第 9–12 周:PLC + OPC UA(**关键分水岭**)

| 周 | 内容 |
|---|---|
| **W9** | PLC 入门:上电接线(24V、输入输出公共端)、编程软件安装、梯形图看懂、DB 块、数据类型与字节序(PLC 是大端!)。自己写"启停 + 计数 + 定时"逻辑。 |
| **W10** | S7.Net Plus / Sharp7 读写 DB 块。**优化块 vs 非优化块**(优化块没有绝对地址,必须关闭优化或用符号访问)、绝对地址计算(`DB1.DBD0`)。PLC ↔ 上位机联调:上位机按钮控制 PLC 输出,PLC 计数回传上位机。 |
| **W11** | OPC UA 深入:UaExpert 浏览地址空间、NodeId 四种类型、Subscription / MonitoredItem / PublishingInterval / SamplingInterval / **Deadband**、队列与丢弃策略、证书信任配置(把客户端证书拷到服务端 `trusted/certs`)、安全策略(None / Basic256Sha256)、用户令牌。用 OPC Foundation `UA-.NETStandard` 写客户端。 |
| **W12** | **自建 OPC UA Server**,把 M1 采集的 Modbus 数据建成信息模型暴露出去。理解 OPC UA PubSub / over MQTT 的定位与适用场景。 |

**产出**
- `Iiot.Drivers.S7` + `Iiot.Drivers.OpcUa`,与 Modbus 一起并入统一抽象 `IDeviceDriver`
- 一台能演示的实体演示台(PLC + 变送器 + 树莓派)

**闸门自测**:对比说明 OPC UA 与 MQTT 的适用场景,解释现场为什么常见"**OPC UA 采集 → MQTT 上云**"这种组合。

> ⚠️ **这一个月是 15k 和 20k 的分界线。** 若进度落后,优先保住本月,砍掉 M4 的 ThingsBoard 与云平台。

---

### M4 · 第 13–16 周:数据平台与可视化

| 周 | 内容 |
|---|---|
| **W13** | TDengine 部署与建模:超级表(STable)/ 子表 / 标签(Tag)的设计、一设备一子表、写入性能测试、降采样查询(`INTERVAL`)、保留策略(`KEEP`)。与 InfluxDB 的选型对比(生态、中文支持、SQL 友好度)。 |
| **W14** | Grafana 接 TDengine 做实时看板 + 告警规则。变量模板做多设备切换。数据压缩与冷热分层策略。 |
| **W15** | ThingsBoard 部署:设备接入 → 物模型 → 规则链 → 仪表盘。理解**属性 / 遥测 / RPC / 设备影子**这套抽象——这是所有 IoT 平台的通用语言。 |
| **W16** | 阿里云 IoT 或华为云 IoTDA **二选一**跑通:物模型定义、设备影子、OTA 升级、规则引擎转发到自建 HTTP 服务。用免费额度即可。 |

**产出**:`docker-compose.yml`(EMQX + TDengine + Grafana)一键起,端到端链路打通

**闸门自测**:设计一张能扛 **500 设备 × 20 点位 × 1s** 的存储模型,并当场算出日写入行数、原始体积、压缩后体积、一年磁盘占用。
> 参考算法:500 × 20 × 86400 = 8.64 亿行/天。若按超级表设计、每行约 20 字节,压缩比按 10:1,约 1.7 GB/天 → 一年约 620 GB。结论:必须做降采样与分级保留(原始数据留 30 天,分钟级留 1 年)。

---

### M5 · 第 17–20 周:整合项目(简历的核心资产)

**项目名:车间设备数采与监控系统**

```
现场设备 (Modbus RTU/TCP · 西门子 S7 · OPC UA)
   │  统一驱动抽象 IDeviceDriver + 点表配置(Excel / JSON 导入)
   ▼
边缘网关 (树莓派 · .NET 8)
   采集调度 → SQLite 缓冲 → 断网续传 → 通信质量统计
   │  MQTT (EMQX) + 物模型 JSON
   ▼
接入服务 (.NET) → TDengine
   └─ 告警引擎(阈值 / 持续时间 / 分级 / 去抖 / 恢复通知)
   │
   ├─ WPF 上位机(实时曲线 · 点位监视 · 手动写值 · 报警列表 · 历史查询)
   └─ Grafana 大屏
```

**加分亮点**(面试官一定会追问的地方)
- OEE 计算(可用率 × 表现性 × 质量率)
- 设备在线率与通信质量统计(哪台设备总丢包,一眼看出)
- 点表热更新(不停机改配置)
- **一键模拟 200 台设备**做压测(证明你考虑过规模)
- 边缘时间 vs 平台时间的时间戳策略(现场设备时钟不准怎么办)

| 周 | 内容 |
|---|---|
| W17–18 | 边缘层 + 平台层打通 |
| W19 | WPF 上位机 UI |
| W20 | 压测、架构图、中文 README、部署脚本、**3–5 分钟演示视频** |

---

### M6 · 第 21–24 周:求职冲刺

| 周 | 内容 |
|---|---|
| **W21** | 简历重写。把 10 年 C# 重新叙事为"**工业软件 / 设备互联**"。每段经历改写成 STAR + 量化数字。突出老底子(串口/Modbus 实战)+ 新项目(完整数采系统)。 |
| **W22** | 面试题库口述训练(见 `interview-qa.md`)。**白板画架构图**练习——10 年经验的人被要求画图的概率接近 100%。 |
| **W23** | 投递。每类目标公司 15–20 家。**先投第二梯队练手**,一周后再投心仪公司。 |
| **W24** | 复盘迭代 + offer 谈判(锚点、13/14 薪、职级、出差补贴、调薪周期)。 |

**成都目标公司分类**
1. **工业软件 / 自动化集成商** — 上位机、SCADA、MES、数采平台。C# 岗位最密集,但可能出差多。
2. **制造业甲方智能制造 / IT 部** — 电子、装备、新能源、军工院所。稳定、少出差,但更看重业务理解和系统落地经验。
3. **能源与市政** — 电力、水务、燃气。Modbus / DL-T645 / 规约类需求多,C# 老系统多,老经验直接吃香。
4. **IoT 平台产品公司** — 作为补充投递。

---

## 3. 学习资源

**规范与文档**(第一手,优先级最高)
- Modbus 官方规范:`Modbus_Application_Protocol_V1_1b3.pdf` + `Modbus_over_serial_line_V1_02.pdf`(modbus.org 免费下载)
- OPC Foundation `UA-.NETStandard` GitHub 仓库的 `Samples/` 目录
- MQTTnet Wiki、EMQX 中文文档、TDengine 官方文档(中文质量高)
- HiveMQ 的 *MQTT Essentials* 系列博客(英文,讲 QoS 最清楚)

**书**
- 《OPC 统一架构》(Mahnke / Leitner / Damm)— OPC UA 权威,啃前四章即可
- 《西门子 S7-1200 PLC 编程及使用指南》— 或所选国产 PLC 的官方手册
- 《数据密集型应用系统设计》第 11 章(流处理)— 理解时序数据与消息语义

**社区**
- 博客园的 C# 上位机圈子(国内这块最活跃)
- B 站:西门子官方号、上位机开发教程
- 知乎"工业自动化""工业互联网"话题

---

## 4. 验证方式:每月末 30 分钟自测

标准是"**能不能对着白板讲出来**",不是"看过没有"。

| 时间点 | 通过标准 |
|---|---|
| M1 末 | 手写 Modbus 03 报文并解释每个字节;真实 485 总线上采到 2 台设备数据 |
| M2 末 | 树莓派服务连跑 72 小时无崩溃;拔网线 10 分钟后数据零丢失续传成功 |
| M3 末 | UaExpert 能浏览你自建的 OPC UA Server;S7 读写 DB 块成功;能讲清 OPC UA vs MQTT |
| M4 末 | docker-compose 一键起全栈;Grafana 看板实时刷新;能算出存储容量规划 |
| M5 末 | 项目 README + 架构图 + 演示视频完成;模拟 200 设备压测通过 |
| M6 末 | 至少 3 场技术面完整走完;拿到 offer 或拿到明确的差距反馈清单 |

---

## 5. 风险预案

| 风险 | 应对 |
|---|---|
| 工作挤占时间,每周只剩 8h | 保底版本:只保 M1 / M2 / M3 / M5,砍掉 M4 的 ThingsBoard 和云平台。周期顺延到 7–8 个月。 |
| M3(PLC + OPC UA)进度落后 | 立刻挪用 M4 的两周。**PLC 与 OPC UA 不可妥协**,这是 15k 与 20k 的分界。 |
| 硬件到货慢 | M1 第 1–2 周先用 Modbus Slave 模拟器 + PLCSIM 推进,不空等。 |
| 学得下去但讲不出来 | 从 M1 开始,每月写一篇技术博客(博客园/掘金)。写作是最好的口述预演,还能当作品集。 |
| 半年后未达 15k | 接 12–15k 岗位,用 1 年真实项目再跳。**不要因为差 2k 而继续待在原地。** |
