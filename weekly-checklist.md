# 24 周逐周任务清单

> 用法:每周开始时看一眼「本周任务」,每周结束时勾选并填「实际投入小时」。
> 每月末做一次闸门自测,结果记到 `progress.md`。
> **勾选标准:能对着白板讲出来 + 代码跑通,不是「看过」。**

---

## M1 · 协议复健 + Modbus 打透

### W1 — 物理层与串口/Socket 复健 (目标 12h)
- [ ] 硬件到货清点,搭好演示台(24V 电源、导轨、端子)
- [ ] 读懂 RS-485 电气规范:差分信号、A/B 极性、120Ω 终端电阻装在总线两端、屏蔽层单端接地
- [ ] 实测:故意不装终端电阻,看长线上的信号质量差异(有示波器更好,没有就看误码率)
- [ ] C# `SerialPort` 写一个最小收发程序,故意制造分包,观察 `DataReceived` 触发次数
- [ ] 写一个「定长 + 超时」的串口分帧器,单元测试覆盖分包/粘包/超时三种情况
- [ ] TCP 粘包复习:`NetworkStream.Read` 返回值必须循环处理,写一个 `ReadExactlyAsync`
- 实际投入:____ h

### W2 — Modbus 规范逐字节 (目标 12h)
- [ ] 下载 `Modbus_Application_Protocol_V1_1b3.pdf` 与 `Modbus_over_serial_line_V1_02.pdf`,通读一遍
- [ ] 手写(纸上)功能码 01 / 02 / 03 / 04 / 05 / 06 / 0F / 10 的请求与响应结构
- [ ] 实现 CRC16(查表法 + 逐位法各一版),用规范里的示例报文验证
- [ ] 装 Modbus Slave 模拟器,用 Modbus Poll 或自写客户端通信
- [ ] Wireshark 抓 Modbus TCP,把 MBAP 头(事务 ID / 协议 ID / 长度 / 单元 ID)逐字段对上
- [ ] 触发一次异常响应(读越界地址),确认返回 `0x83 + 异常码 02`
- 实际投入:____ h

### W3 — 手写 Modbus 库 (目标 12h)
- [ ] `Iiot.Drivers.Modbus`:报文编解码(不依赖任何第三方库)
- [ ] RTU 与 TCP 两种传输的抽象分离(`IModbusTransport`)
- [ ] 超时 + 重试 + 异常码转业务异常
- [ ] **批量读优化**:把离散地址按最大跨度合并成最少请求(这是面试加分点)
- [ ] 32 位数据的四种字序(ABCD / CDAB / BADC / DCBA)可配置,写测试覆盖
- [ ] 单元测试:CRC、字序、编解码、异常路径,覆盖率 > 70%
- 实际投入:____ h

### W4 — 轮询调度与真实总线 (目标 12h)
- [ ] 多设备轮询调度器:每设备独立周期 + 优先级
- [ ] 慢设备/离线设备隔离,不拖垮整条总线
- [ ] 掉线后指数退避重连(1s → 2s → 4s → 上限 30s)
- [ ] 通信质量统计:成功率、平均/P99 时延、超时次数、连续失败次数
- [ ] 真实 485 总线挂 2–3 个从站,跑通 24 小时
- [ ] **写《Modbus 现场排错手册》**:接线错、地址错位、波特率不匹配、字序错、终端电阻缺失、地址冲突,每条写「现象 → 判断方法 → 处理」
- 实际投入:____ h

### 🚦 M1 闸门自测
- [ ] 合上文档,纸上写出 FC03 请求 + 响应的完整字节结构,解释每个字节
- [ ] 解释 CRC 为什么低字节在前(而 MBAP 头是大端)
- [ ] 真实 485 总线上稳定采集 2 台以上设备

---

## M2 · MQTT + Linux 边缘网关

### W5 — 树莓派与 Linux 基本功 (目标 12h)
- [ ] 树莓派装 Ubuntu Server,SSH 免密登录
- [ ] `dotnet publish -r linux-arm64 --self-contained` 部署上去跑起来
- [ ] 写 systemd unit:开机自启、崩溃自动重启、日志进 journald
- [ ] udev 规则按 USB 序列号固定串口名(`/dev/ttyModbus0`),拔插不变
- [ ] 排障工具练手:`ip a`、`ss -tunlp`、`journalctl -u`、`tcpdump -i eth0 port 502`
- [ ] Docker 装好,跑通 `hello-world` 与端口映射
- 实际投入:____ h

### W6 — MQTT 原理与 EMQX (目标 12h)
- [ ] Docker 起 EMQX,进 Dashboard 看连接与订阅
- [ ] 读完 HiveMQ *MQTT Essentials* 系列
- [ ] 用 MQTTnet 写发布/订阅 demo,分别用 QoS 0 / 1 / 2 抓包看报文交互次数
- [ ] LWT 实验:客户端异常断开后,遗嘱消息多久发出、怎么配 Keep Alive
- [ ] Retain 实验:新订阅者立刻收到最后一条
- [ ] 共享订阅 `$share/g1/topic` 实验:两个消费者负载分担
- [ ] **定下自己的主题设计规范**并写进文档:`iiot/{site}/{line}/{device}/telemetry|status|cmd|cmd_resp`
- 实际投入:____ h

### W7 — 断网续传(本月核心) (目标 12h)
- [ ] 采集数据先落 SQLite(带 `sent` 标记 + 自增序号)
- [ ] 发送线程:未发送数据按序号批量发布,收到 ACK 后标记/删除
- [ ] `System.Threading.Channels` 有界队列 + 满时策略(丢最旧 / 落盘)
- [ ] 内存上限验证:断网 1 小时,内存不无限增长
- [ ] **拔网线测试**:断 10 分钟后恢复,数据零丢失、顺序正确、无重复(或下游可去重)
- [ ] 录一段拔网线演示视频(面试直接放)
- 实际投入:____ h

### W8 — 可靠性打磨 (目标 12h)
- [ ] Polly:重试 + 熔断 + 超时策略组合
- [ ] 健康检查端点(采集是否正常、Broker 是否连上、积压条数)
- [ ] 优雅退出:`IHostApplicationLifetime`,退出前把缓冲刷盘
- [ ] 配置热加载 `IOptionsMonitor`,改点表不重启
- [ ] Serilog 结构化日志 + 按天滚动 + 大小上限
- [ ] 通过 MQTT 下发远程配置(改采集周期、改点表)
- [ ] 连续运行 72 小时不崩溃,内存曲线平稳
- 实际投入:____ h

### 🚦 M2 闸门自测
- [ ] 树莓派服务连跑 72h 无崩溃、无内存泄漏
- [ ] 拔网线 10 分钟,恢复后数据零丢失
- [ ] 讲清 QoS1 重复投递的成因,以及业务侧幂等方案

---

## M3 · PLC + OPC UA(关键分水岭)

### W9 — PLC 入门 (目标 12h)
- [ ] PLC 上电接线:24V 电源、输入公共端、输出公共端、按钮与指示灯
- [ ] 装编程软件(汇川 AutoShop / 信捷 XDPPro / 西门子 TIA Portal)
- [ ] 看懂梯形图基本指令:常开常闭、线圈、自锁、定时器、计数器
- [ ] 自己写:按钮启动 → 输出保持 → 停止按钮复位 → 运行时长计时 → 启动次数计数
- [ ] 理解 DB 块 / M 区 / I 区 / Q 区的区别与地址表示法
- [ ] **牢记 PLC 是大端**,和 x86 小端的转换关系
- 实际投入:____ h

### W10 — S7 通信与联调 (目标 12h)
- [ ] `S7.Net Plus` 或 `Sharp7` 连上 PLC,读一个 DB 块
- [ ] 踩一遍**优化块**的坑:优化块读不到绝对地址 → 关闭「优化的块访问」
- [ ] 手算绝对地址:`DB1.DBX0.0`(位) / `DB1.DBW2`(字) / `DB1.DBD4`(双字)
- [ ] 写值:上位机按钮 → PLC 输出灯亮
- [ ] 读值:PLC 计数器 → 上位机实时显示
- [ ] 封装 `Iiot.Drivers.S7`,接口与 Modbus 驱动对齐
- 实际投入:____ h

### W11 — OPC UA 客户端 (目标 12h)
- [ ] 装 UaExpert,连一个公开测试服务器或本地模拟服务器,浏览地址空间
- [ ] 理解 NodeId 四种类型(Numeric / String / GUID / Opaque)与 NamespaceIndex
- [ ] 用 `UA-.NETStandard` 写客户端:连接 → Browse → Read → Write
- [ ] Subscription + MonitoredItem:调 PublishingInterval / SamplingInterval / QueueSize
- [ ] **Deadband 实验**:绝对死区设 0.5,看变化小于 0.5 时不上报
- [ ] 证书踩坑:从 None 切到 Basic256Sha256,把客户端证书拷进服务端 `trusted/certs`
- [ ] 封装 `Iiot.Drivers.OpcUa`
- 实际投入:____ h

### W12 — 自建 OPC UA Server + 统一抽象 (目标 12h)
- [ ] 用 `UA-.NETStandard` 起一个 Server,把 Modbus 采到的数据建成节点暴露
- [ ] 信息模型:按「产线 → 设备 → 变量」建层级,不要一堆平铺变量
- [ ] 用 UaExpert 连自己的 Server,能浏览、能订阅
- [ ] **统一驱动抽象 `IDeviceDriver`**:Modbus / S7 / OPC UA 三个实现全部接入
- [ ] 点表配置文件(JSON)驱动,加设备不改代码
- [ ] 整理「OPC UA vs MQTT」对比笔记,准备面试口述
- 实际投入:____ h

### 🚦 M3 闸门自测(**不可妥协**)
- [ ] UaExpert 能浏览并订阅你自建的 OPC UA Server
- [ ] S7 DB 块读写双向成功,上位机与 PLC 联动演示
- [ ] 口述:OPC UA 与 MQTT 的定位差异,以及「OPC UA 采 → MQTT 上云」的原因

---

## M4 · 数据平台与可视化

### W13 — TDengine 建模 (目标 12h)
- [ ] Docker 起 TDengine,跑通建库建表写入查询
- [ ] 超级表设计:一类设备一张超级表,一台设备一张子表,标签放 site/line/device/type
- [ ] 写入性能测试:单条 vs 批量 vs 参数绑定,记录 TPS 差异
- [ ] 降采样查询:`INTERVAL(1m)` + `AVG/MAX/MIN`
- [ ] 保留策略 `KEEP` 与多级存储
- [ ] 写一页「TDengine vs InfluxDB 选型对比」
- 实际投入:____ h

### W14 — Grafana 看板与告警 (目标 12h)
- [ ] Grafana 装 TDengine 数据源插件,连上
- [ ] 做一块车间看板:实时曲线、当前值面板、设备在线状态
- [ ] 模板变量:下拉切换产线 / 设备
- [ ] 告警规则:阈值 + 持续时间 + 通知渠道
- [ ] 数据分层策略文档:原始 30 天 / 分钟级 1 年 / 小时级 3 年
- 实际投入:____ h

### W15 — ThingsBoard 与物模型抽象 (目标 12h)
- [ ] Docker 起 ThingsBoard,创建设备、拿 Access Token
- [ ] 用 MQTT 上报遥测,在仪表盘看到实时数据
- [ ] 规则链:数据进来 → 阈值判断 → 产生告警 → 发通知
- [ ] 搞清四个概念的区别:属性(attributes)/ 遥测(telemetry)/ RPC / 设备影子
- [ ] 对照自己的项目,想清楚「我自建的这套和 ThingsBoard 的差别在哪、为什么不直接用它」(面试必问)
- 实际投入:____ h

### W16 — 云平台跑通(二选一) (目标 12h)
- [ ] 选定:阿里云 IoT 平台 ☐ / 华为云 IoTDA ☐
- [ ] 定义物模型(属性 / 服务 / 事件)
- [ ] 设备用 MQTT 接入,三元组鉴权跑通
- [ ] 设备影子:离线时下发指令,上线后同步
- [ ] OTA:走一遍升级包上传与下发流程
- [ ] 规则引擎:数据转发到自建 HTTP 服务
- 实际投入:____ h

### 🚦 M4 闸门自测
- [ ] `docker-compose up` 一键起 EMQX + TDengine + Grafana,链路端到端通
- [ ] 白板上算出 500 设备 × 20 点位 × 1s 的日写入量与一年磁盘占用,给出分层保留方案

---

## M5 · 整合项目

### W17 — 边缘层整合 (目标 12h)
- [ ] `IDeviceDriver` 三个实现全部接入统一采集调度
- [ ] 点表从 Excel / JSON 导入,支持设备、点位、类型、缩放、字序、报警上下限
- [ ] 采集 → 缓冲 → MQTT 上报全链路跑通
- [ ] 通信质量统计上报(每台设备的成功率与时延)
- 实际投入:____ h

### W18 — 平台层整合 (目标 12h)
- [ ] .NET 接入服务订阅 MQTT,批量写入 TDengine
- [ ] 告警引擎:阈值 / 持续时间 / 分级 / 去抖(防抖动误报)/ 恢复通知
- [ ] 时间戳策略:边缘时间为准 + 平台接收时间双写,处理设备时钟漂移
- [ ] 补传数据与实时数据混流的处理(乱序写入)
- 实际投入:____ h

### W19 — WPF 上位机 (目标 12h)
- [ ] WPF + CommunityToolkit.Mvvm 骨架
- [ ] 实时曲线(ScottPlot / LiveCharts2),1000 点以上不卡
- [ ] 点位监视表格,通信状态颜色标识
- [ ] 手动写值(带二次确认与操作审计日志)
- [ ] 报警列表 + 确认 + 历史查询
- 实际投入:____ h

### W20 — 打磨与包装 (目标 12h)
- [ ] **200 台设备模拟器**,一键压测,记录 CPU / 内存 / 写入延迟
- [ ] 架构图(用 draw.io 或 Mermaid,要能在白板上默画出来)
- [ ] README:背景、架构、技术选型理由、部署步骤、压测结果
- [ ] 部署脚本 / docker-compose 一键起
- [ ] **3–5 分钟演示视频**:实物 → 采集 → 断网续传 → 看板 → 告警
- [ ] 代码传 GitHub / Gitee(注意脱敏)
- 实际投入:____ h

### 🚦 M5 闸门自测
- [ ] 200 设备压测通过,给得出资源占用数字
- [ ] 不看图能在白板上默画完整架构并讲 5 分钟
- [ ] 演示视频完成

---

## M6 · 求职冲刺

### W21 — 简历重写 (目标 12h)
- [ ] 一句话定位:「10 年 C#,专注工业软件与设备互联,熟悉 Modbus / S7 / OPC UA / MQTT,能独立交付数采与监控系统」
- [ ] 每段经历改 STAR + 量化(采集多少设备、多少点位、多快周期、稳定性多少)
- [ ] 把老经历重新叙事:当年的串口/Modbus 项目 → 「工业设备数据采集」
- [ ] 项目栏放 M5 项目,配 GitHub 链接与演示视频链接
- [ ] 技能栏分层写:协议 / 边缘 / 平台 / 上位机,不要一行罗列 30 个词
- [ ] 找一位做自动化的朋友帮看一遍(没有就发到社区求评)
- 实际投入:____ h

### W22 — 面试训练 (目标 12h)
- [ ] 通读 `interview-qa.md`,每题**出声讲一遍**,卡壳的标红重学
- [ ] 白板画架构图练习 ×5 次,每次限时 5 分钟
- [ ] 准备 3 个「踩坑故事」:一个协议层的、一个可靠性的、一个现场的
- [ ] 准备反问问题(问团队结构、技术债、出差比例、考核方式)
- [ ] 模拟面试一场(朋友或 AI 陪练)
- 实际投入:____ h

### W23 — 投递 (目标 12h)
- [ ] 列成都目标公司清单,四类各 15–20 家
- [ ] 第一批:第二梯队 10 家(练手,不心疼)
- [ ] 复盘第一批面试反馈,改简历、补短板
- [ ] 第二批:心仪公司 15 家
- [ ] 每场面试当天写复盘:问到什么、哪里卡壳、怎么补
- 实际投入:____ h

### W24 — 收口与谈判 (目标 12h)
- [ ] 继续面试 + 复盘迭代
- [ ] 谈判准备:期望区间锚点、13/14 薪、职级、调薪周期、出差补贴、加班情况
- [ ] 手上有两个 offer 再谈,不要单点谈判
- [ ] 若未达标:按后备路径接 12–15k,同时定下一年的提升目标
- 实际投入:____ h

### 🚦 M6 闸门自测
- [ ] 至少 3 场完整技术面
- [ ] 拿到 offer,或拿到一份明确的「还差什么」清单
